# Vision: stub (default, no models) | insightface (production)
VISION_BACKEND=stub
REID_INTERVAL_S=30
MISS_THRESHOLD=3
COSINE_THRESHOLD=0.45
ENROLLMENT_JSON=enrollments.json
ALLOW_ORIGINS=*

# Supabase (server-side only; never ship the service role key to the browser)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
DATABASE_URL=
